// var preco = 2;
const PRECO = 2;
var desconto = 0.2;

var total = PRECO * (1 - desconto)

PRECO = PRECO * (1 - desconto)

// Delcarando funcoes
function soma(a, b) {
    // console.log(a + b) -> usamos quando queremos ver o valor no console para saber se esta funcionando
    return a + b
}

soma(3, 5) // 8

print(soma(1, 2))